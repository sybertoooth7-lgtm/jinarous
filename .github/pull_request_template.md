## What does this change?

A short description of what this PR does and why.

## How was this tested?

- [ ] Ran `npm run lint` (backend and/or frontend, as relevant)
- [ ] Ran `npm test` (backend) and it passes
- [ ] Ran `npm run build` (frontend) and it succeeds
- [ ] Manually tested the change locally

Describe any manual testing steps that aren't obvious from the diff.

## Does this touch anything security-sensitive?

Auth, sessions, CSRF, rate limiting, password/token handling, or
anything else covered in [SECURITY.md](SECURITY.md)?

- [ ] No
- [ ] Yes — explain below

## Database changes?

- [ ] No migration needed
- [ ] New migration added (`backend/migrations/`), numbered
      sequentially, using `IF NOT EXISTS`/`IF EXISTS` guards

## Screenshots (if UI-facing)

Before/after screenshots make frontend PRs much faster to review.
